# Secrets
.env

# Python
.venv/
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# Cached openFDA responses and model files (regenerate with `rxclarify ingest`)
data/raw/
.fastembed_cache/

# Generation-run artifacts: ~400 KB each, because they embed every retrieved
# excerpt's full text — corpus data that already lives in Postgres. Regenerate
# for about $0.025 with:
#     python eval/harness.py --config hybrid --mode generate
# The small *_retrieval.json and *_faithfulness.json files ARE committed: they
# are the evidence behind the results table in README.md.
eval/results/*_generate.json

# Editors / OS
.vscode/
.idea/
Thumbs.db
.DS_Store
